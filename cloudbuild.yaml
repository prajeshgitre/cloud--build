steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build', 
      '-t', 
      '	asia-south1-docker.pkg.dev/prajesh/my-repo/my-app-2:$COMMIT_SHA', 
      '.'
    ]

  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'push', 
      '	asia-south1-docker.pkg.dev/prajesh/my-repo/my-app-2:$COMMIT_SHA'
    ]

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: [
      'run', 
      'deploy', 
      'my-app-1',  # The name of your Cloud Run service
      '--image', 
      'u	asia-south1-docker.pkg.dev/prajesh/my-repo/my-app-2:$COMMIT_SHA', 
      '--platform',
      'managed',
      '--region',
      'us-central1',  # The region where you want to deploy
      '--allow-unauthenticated'  # If you want to allow public access
    ]

images:
  - 'asia-south1-docker.pkg.dev/prajesh/my-repo/my-app-2:$COMMIT_SHA'

logsBucket: 'gs://cloud-build-01'
