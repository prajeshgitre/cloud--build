steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build', 
      '-t', 
      'asia-south1-docker.pkg.dev/prajesh/my-app2/my-app-1:$COMMIT_SHA', 
      '.'
    ]

  # Step 2: Push the Docker image to Artifact Registr
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'push', 
      'asia-south1-docker.pkg.dev/prajesh/my-app2/my-app-1:$COMMIT_SHA'
    ]

  # Step 3: Create a Cloud Deploy release
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: [
      'deploy', 
      'releases', 
      'create', 
      'my-release-$SHORT_SHA',  # Shorten the release name
      '--delivery-pipeline', 
      'my-run-demo-app-1', 
      '--region', 
      'asia-south1',
      '--images',
      'my-app-1=asia-south1-docker.pkg.dev/prajesh/my-app2/my-app-1:$COMMIT_SHA'
    ]

  # Step 4: Deploy the image to Cloud Run using Cloud Deploy
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: [
      'run', 
      'deploy', 
      'my-app-1',  
      '--image', 
      'asia-south1-docker.pkg.dev/prajesh/my-app2/my-app-1:$COMMIT_SHA', 
      '--platform',
      'managed',
      '--region',
      'asia-south1',
      '--allow-unauthenticated'
    ]

images:
  - 'asia-south1-docker.pkg.dev/prajesh/my-app2/my-app-1:$COMMIT_SHA'

logsBucket: 'gs://cloud-build-03'
